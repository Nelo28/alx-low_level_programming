#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry Point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	putchar(d);
	putchar(p);
	if (d == '8' && p == '9')
		continue;

	putchar(',');
	putchar(' ');
}
